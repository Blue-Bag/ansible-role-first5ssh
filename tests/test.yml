---
- hosts: all

  pre_tasks:

   - name: Install openssl
     apt:
       name: "{{ item }}"
       update_cache: yes
       cache_valid_time: 86400
     with_items:
       - openssh-server
       - openssh-client
       - openssh

  roles:
    - role_under_test