---
- hosts: all

  pre_tasks:

   - name: Install openssl
     apt:
       name: ['openssh-server','openssh-client']
       update_cache: yes
       cache_valid_time: 86400
  roles:
    - role_under_test